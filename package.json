{
  "name": "aircanada-playwright-e2e",
  "version": "1.0.0",
  "description": "Portfolio-quality Playwright E2E smoke tests for Air Canada booking flow",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test booking.smoke.spec.ts",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0"
  }
}
